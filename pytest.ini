[pytest]
testpaths = tests

# Third-party deprecation noise only. ezdxf is pulled in by CadQuery and its
# pyparsing warnings say nothing about this project. Warnings raised by our own
# code are deliberately left visible.
filterwarnings =
    ignore::DeprecationWarning:ezdxf.*
    ignore::DeprecationWarning:pyparsing.*
